# 提交前自检清单

- 当前分支符合 git flow 预期，不是在 `main` 或 `dev` 上直接做日常提交
- 本次暂存内容只包含当前任务需要的改动
- 拟定的 commit message 符合 angular commit
- commit message 不使用模糊或自由格式标题
- 已准备 `Co-authored-by: opencode-agent[bot] <opencode-agent[bot]@users.noreply.github.com>`
- 已明确向用户说明将执行 commit、拟用 message 与 co-author trailer
- 已收到用户确认；如果没有，停止在提交前
